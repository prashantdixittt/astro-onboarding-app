/**
 * Email Configuration
 *
 * Control email sending behavior globally from this file
 */

const emailConfig = {
  // Set to true to enable automatic email sending, false to disable
  ENABLE_EMAIL_SENDING: true,

  // Recipient email address
  RECIPIENT_EMAIL: 'yadavshashank700@gmail.com',

  // Send email automatically when results are shown (no button click required)
  AUTO_SEND_ON_RESULTS: true,
};

export default emailConfig;